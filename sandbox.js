var character = 'Hai'; // this variable can now only be a string
var age = 30; // age only now hold a number
var isRich = false; // isRich now hold a boolean
// a function
var circ = function (diameter) {
    return diameter * Math.PI;
};
console.log(circ(7.5));
